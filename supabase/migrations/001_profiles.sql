-- ============================================================================
-- O modelo atual do banco está em supabase/scripts/
--   1) wipe_de_match.sql    — apaga tabelas e funções do projeto (com IF EXISTS)
--   2) recreate_de_match.sql — cria tudo de novo (somente CREATE, texto em português)
--
-- Este arquivo não aplica mudanças; evita rodar um modelo antigo (inglês) por engano.
-- ============================================================================
SELECT 1;
